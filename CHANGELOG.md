# 변경 이력

## 1.2.0 - 2026-08-04

- README 기준 `stream_processor/`, `sequencing_engine/`, `api/`, `batch/`, `vector_search/`, `common/` 구조로 리팩터링
- 루트 `app.py`는 기존 Uvicorn 실행 명령 호환 진입점으로 유지
- 가상 라이더 후보 4명을 화면 시나리오에 연결
- 거절 라이더 제외 후 다음 후보 자동 점수화·재제안
- 현재 제안 대상 라이더만 수락·거절할 수 있도록 검증
- 제안 응답시간 만료 시 다음 라이더 자동 재배차
- 모든 후보 소진 시 `NO_RIDER_AVAILABLE` fallback 상태 추가
- 고객·사장님·라이더 화면에 재배차 진행 상태 표시
- 통합 시연 화면의 라이더 iframe을 현재 제안 대상에 맞게 자동 전환
- 자동 재배차 상세 문서와 테스트 추가
- 전체 자동 테스트 18개 통과

## 1.1.1 - 2026-08-04

- 결정론적 가상 데이터 생성 스크립트 추가
- 매장 12개, 고객 36명, 라이더 8명, 과거 주문·조리 이력 각 80건 생성
- 평시·우천 피크·매장 지연·전체 픽업 우선 시나리오 추가
- `.env`의 `DUMMY_DATASET`으로 시작 시나리오 선택 가능
- `/demo` 화면에서 가상 데이터 세트 실시간 전환 가능
- 데이터 세트 조회·변경 REST API 추가
- 가상 데이터 생성 및 적용 가이드 추가


## 1.1.0

- 기존 혼합 픽업·배달 경로 유지
- `전체 픽업 후 배달` 경로 전략 추가
- 시연 콘솔에서 두 경로 전략 전환 기능 추가
- 고객·사장님·라이더 화면에 현재 경로 전략 표시
- 네이버 지도 JavaScript API 어댑터 추가
- Google Maps JavaScript API 어댑터 추가
- 외부 지도 키 누락·로딩 실패 시 기존 추상 지도 자동 fallback
- 지도 제공자 설정 API 추가
- 경로 전략 및 지도 관련 자동 테스트 추가
